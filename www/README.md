The driver UI lives in `index.html` (single-file Capacitor web app).

If this folder only has `driver-supabase-integration.js`, upload `www/index.html` from the local project:

https://github.com/appinnovationmc-stack/eesyload-driver/upload/main/www

`index.html` must sit next to `driver-supabase-integration.js`.
